# 📦 npm 发布步骤总结

## ✅ 当前状态检查

- ✅ 项目已构建（dist 目录包含所有必需文件）
- ✅ package.json 配置正确
- ✅ 包名 `megaview-ui` 可用（未在 npm 上注册）
- ✅ 已配置 `publishConfig.access: "public"`
- ⚠️  需要登录 npm（当前未登录）

## 🚀 立即发布（三步）

### 第 1 步：登录 npm

在终端运行：

```bash
npm login
```

按提示输入：
- **Username**: 你的 npm 用户名
- **Password**: 你的 npm 密码（输入时不会显示）
- **Email**: 你的邮箱地址

> 💡 如果没有 npm 账号，请先访问 https://www.npmjs.com/signup 注册

### 第 2 步：确认构建（可选）

如果刚才没有构建，运行：

```bash
npm run build
```

### 第 3 步：发布

```bash
npm publish
```

> ✅ 由于已配置 `publishConfig.access: "public"`，无需添加 `--access public` 参数

## 📋 发布后验证

发布成功后，你可以：

1. **访问 npm 网站查看**：
   ```
   https://www.npmjs.com/package/megaview-ui
   ```

2. **通过 CDN 使用**（发布后立即生效）：
   ```html
   <!-- jsDelivr CDN -->
   <script type="module" src="https://cdn.jsdelivr.net/npm/megaview-ui@1.0.0/dist/megaview-ui.es.js"></script>
   
   <!-- unpkg CDN -->
   <script type="module" src="https://unpkg.com/megaview-ui@1.0.0/dist/megaview-ui.es.js"></script>
   ```

3. **通过 npm 安装**：
   ```bash
   npm install megaview-ui
   ```

## 🎯 一键发布命令

如果你想一次性执行所有步骤：

```bash
# 1. 登录（首次需要）
npm login

# 2. 构建并发布
npm run build && npm publish
```

## 📝 将要发布的文件

根据预览，将发布以下文件：

- ✅ `dist/megaview-ui.es.js` (11.0 kB) - ES Module 格式
- ✅ `dist/megaview-ui.umd.js` (9.8 kB) - UMD 格式
- ✅ `dist/megaview-ui.iife.js` (9.7 kB) - IIFE 格式
- ✅ `README.md` (6.0 kB) - 文档
- ✅ `DEPLOYMENT.md` (5.6 kB) - 部署指南
- ✅ `package.json` (1.2 kB) - 包配置

**总大小**: 9.1 kB (压缩后) / 43.4 kB (解压后)

## 🔄 后续更新版本

当需要发布新版本时：

```bash
# 更新版本号（会自动更新 package.json）
npm version patch   # 1.0.0 -> 1.0.1
# 或
npm version minor   # 1.0.0 -> 1.1.0
# 或
npm version major   # 1.0.0 -> 2.0.0

# 发布（会自动构建，因为配置了 prepublishOnly）
npm publish
```

## ⚠️ 注意事项

1. **首次发布作用域包**：已配置 `publishConfig.access: "public"`，无需担心
2. **版本号**：当前版本是 `1.0.0`，首次发布可以使用
3. **文件大小**：包大小约 9.1 kB，非常轻量
4. **CDN 延迟**：发布后，CDN 可能需要几分钟同步

## 📚 相关文档

- **快速发布指南**: [QUICK_PUBLISH.md](./QUICK_PUBLISH.md)
- **详细发布指南**: [PUBLISH_GUIDE.md](./PUBLISH_GUIDE.md)
- **部署指南**: [DEPLOYMENT.md](./DEPLOYMENT.md)
- **CDN 示例**: [examples/cdn/index.html](./examples/cdn/index.html)

## 🎉 发布成功后

发布成功后，你的组件库就可以：

1. ✅ 通过 `npm install megaview-ui` 安装
2. ✅ 通过 jsDelivr/unpkg CDN 直接使用
3. ✅ 在任何支持 npm 的项目中使用
4. ✅ 在任何现代浏览器中通过 CDN 使用

---

**准备好了吗？运行 `npm login` 开始发布吧！** 🚀

