---
alwaysApply: true
---

# 代码编写规则

### 1. 中文注释要求

- **必须要求**：每次写代码时都必须用中文充分注释

- **面向对象**：写项目和看项目的人都是第一次写 Web Components 的新手

- **注释内容**：需要尽可能详细描述组件的设计思想和代码的目的

### 2. 注释规范

- **类注释**：每个 Web Components 类都要有中文注释，说明这个组件是做什么的，为什么需要这个组件

- **生命周期方法注释**：每个生命周期方法（constructor、connectedCallback、disconnectedCallback、attributeChangedCallback）都要说明其作用和调用时机

- **方法注释**：每个方法都要有中文注释，说明这个方法的功能、参数、返回值和使用场景

- **属性注释**：observedAttributes 中的每个属性都要注释说明其用途、类型和默认值

- **变量注释**：重要变量需要注释说明其作用和数据类型

- **逻辑注释**：复杂的业务逻辑要有注释解释为什么要这么做

### 3. Web Components 组件设计说明

- **设计思想**：在组件类开头用注释详细说明这个组件的设计理念

- **功能描述**：说明组件能做什么，不能做什么

- **使用场景**：描述这个组件在什么情况下使用，支持哪些框架（React、Vue、原生 HTML）

- **依赖关系**：说明这个组件依赖哪些其他组件或库

- **Shadow DOM 说明**：如果使用 Shadow DOM，要说明为什么使用以及样式隔离的好处

- **自定义事件说明**：如果组件派发自定义事件，要详细说明事件名称、事件数据结构和触发时机

### 4. 代码目的说明

- **为什么要写这段代码**：每段重要代码都要说明其存在的原因

- **解决什么问题**：说明这段代码解决了什么具体问题

- **如何工作的**：用简单的语言解释代码的工作原理

- **Web Components 特性说明**：使用到 Web Components 特性（如 observedAttributes、Shadow DOM、自定义事件）时，要解释这些特性的作用

### 5. 新手友好原则

- **避免专业术语**：如果必须使用（如 Shadow DOM、Custom Elements、生命周期钩子），要附带简单的类比解释

- **步骤拆分**：复杂逻辑要拆分成小步骤，并逐一注释

- **示例说明**：重要的概念可以用代码示例来说明

- **Web Components 概念解释**：遇到 Web Components 特有的概念时，要用通俗的语言解释

### 6. 组件库特殊要求

- **API 文档注释**：公共方法（如 updateData）要有详细的注释，说明参数类型、返回值和用法示例

- **属性变化处理**：attributeChangedCallback 中的逻辑要详细注释，说明每个属性的变化会触发什么操作

- **事件系统说明**：自定义事件的派发和监听要注释说明，包括事件名称、事件对象结构和使用示例

- **样式隔离说明**：Shadow DOM 的样式要注释说明为什么这样设计，以及如何与外部样式隔离

- **数据格式说明**：组件接受的数据格式要有详细注释，包括数据结构、必需字段和可选字段

### 7. 审查标准

- **注释完整性**：新提交的代码如果缺少中文注释，必须打回修改

- **描述准确性**：注释内容必须准确反映代码的实际功能和设计思想

- **易懂性**：注释要让第一次写 Web Components 的人也能看懂

- **Web Components 特性覆盖**：所有使用的 Web Components 特性都要有相应注释

### 8. 代码注释一致性维护

- **同步更新要求**：每次修改代码功能时，必须同时更新对应的中文注释

- **内容匹配**：注释必须准确描述当前代码的实际行为和逻辑

- **版本同步**：代码的任何变更都要反映在注释中，避免注释滞后

- **审查要点**：代码审查时要检查注释是否与代码实现保持一致

- **API 变更同步**：如果修改了组件的公共 API（属性、方法、事件），必须同步更新相关注释

